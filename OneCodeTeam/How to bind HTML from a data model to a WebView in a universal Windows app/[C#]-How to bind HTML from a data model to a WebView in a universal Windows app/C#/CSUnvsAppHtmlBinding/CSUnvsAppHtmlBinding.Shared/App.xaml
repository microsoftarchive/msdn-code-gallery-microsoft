<Application
    x:Class="CSUnvsAppHtmlBinding.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CSUnvsAppHtmlBinding">
    <Application.Resources>
        <ResourceDictionary>

            <x:String x:Key="AppLogo">Universal Code Samples by All-In-One Code Framework</x:String>
            <x:String x:Key="AppName">Universal app: Bind HTML to a WebView with Attached Properties</x:String>
            <x:String x:Key="AppSubtitle">
                This sample demonstrates how to create an Attached Property to allow data-binding an HTML string to a WebView.
            </x:String>
            <x:String x:Key="AppDescription">
            </x:String>

            <Style x:Key="AppHeaderStyle" TargetType="TextBlock" BasedOn="{StaticResource HeaderTextBlockStyle}">
                <Setter Property="TextWrapping" Value="Wrap"></Setter>
                <Setter Property="Margin" Value="0,10,0,10"></Setter>
                <Setter Property="LineHeight" Value="55"/>
            </Style>

            <Style x:Key="AppLogoStyle" TargetType="TextBlock" BasedOn="{StaticResource TitleTextBlockStyle}">
                <Setter Property="TextWrapping" Value="Wrap"></Setter>
            </Style>

            <Style x:Key="AppSubtitleStyle" TargetType="TextBlock" BasedOn="{StaticResource SubheaderTextBlockStyle}">
                <Setter Property="TextWrapping" Value="Wrap"></Setter>
                <Setter Property="Margin" Value="0,10,0,10"></Setter>
            </Style>
            <Style x:Key="AppDescriptionStyle" TargetType="TextBlock" BasedOn="{StaticResource BodyTextBlockStyle}">

            </Style>

            <Style x:Key="AppBodyTextStyle" TargetType="TextBlock" BasedOn="{StaticResource BodyTextBlockStyle}">
                <Setter Property="TextWrapping" Value="Wrap"></Setter>
            </Style>

            <Style x:Key="AppFooterStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="TextWrapping" Value="Wrap"></Setter>
                <!--
                Set ThemeResource reference to the resource. So that the resource can dynamically change depending 
                on current theme.
                -->
                <Setter Property="Foreground" Value="{ThemeResource ApplicationSecondaryForegroundThemeBrush}"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>

</Application>
