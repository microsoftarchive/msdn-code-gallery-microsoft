<Page
    x:Class="SDKSample.SimplePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SDKSample"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}" Margin="10,10,10,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="GuidText" 
                   Style="{StaticResource BasicTextStyle}" Grid.Row="0" Grid.Column="0" FontWeight="Bold" />
        <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,10,0,0"
                   Style="{StaticResource BasicTextStyle}" 
                   TextWrapping="WrapWholeWords" FontFamily="Global User Interface">
            <Run>By default, everytime that the app navigates to page, a new instance of the page is created.</Run>
            <LineBreak/>
            <LineBreak/>
            <Run> Use the buttons to Navigate to new pages, go back, go forward, go home or clear the stacks.</Run>
            <LineBreak/>
            <LineBreak/>
            <Run>Take a look to the info section to learn about the navigation model behavior.</Run>
        </TextBlock>
    </Grid>
</Page>