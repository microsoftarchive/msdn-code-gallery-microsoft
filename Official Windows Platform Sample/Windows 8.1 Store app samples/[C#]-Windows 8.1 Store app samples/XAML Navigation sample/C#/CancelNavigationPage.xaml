<Page
    x:Class="Navigation.CancelNavigationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Navigation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}" Margin="10,10,10,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <ToggleSwitch Header="Cancel Navigation"  x:Name="CancelNavigationSwitch"  Grid.Row="0" />
        <TextBlock x:Name="IDText" 
                   Grid.Row="1" 
                   Style="{StaticResource BasicTextStyle}"  FontWeight="Bold"  />

        <TextBlock TextWrapping="WrapWholeWords"
                   Grid.Row="2" 
                   Text="Use the ToggleSwitch to cancel/activate the navigation. Click on the Navigate button to verify navigation behavior."
                  Style="{StaticResource BasicTextStyle}"  />

    </Grid>
</Page>