<Page
    x:Class="Navigation.PageWithParameters"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Navigation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="MainGrid" Background="{StaticResource ApplicationPageBackgroundThemeBrush}" Margin="10,10,10,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="MessageText" 
                   Style="{StaticResource BasicTextStyle}" Grid.Row="0" Grid.Column="0" FontWeight="Bold" />
        <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,10,0,0"
                   Style="{StaticResource BasicTextStyle}" 
                   TextWrapping="WrapWholeWords">
            <Run>By default, every time the app navigates to page, a new instance of the page is created.</Run>
            
        </TextBlock>
    </Grid>
</Page>